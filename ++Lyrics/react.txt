import React, { useEffect, useState } from 'react';
import Button from 'react-bootstrap/Button';
import Card from 'react-bootstrap/Card';
import "./about.css";

const About = () => {
    const [imageFilenames, setImageFilenames] = useState([]);
  
    useEffect(() => {
      // Fetch the list of image filenames from your directory here
      // You can use a server-side API or a client-side method to get the filenames
      // For simplicity, let's assume you have an array of filenames like this:
      const filenames = ["image1.jpg", "image2.jpg", "image3.jpg"];
      setImageFilenames(filenames);
    }, []);
  
    return (
      <div className='contents'>
        {imageFilenames.map((filename, index) => (
          <Card key={index} style={{ width: '18rem' }}>
            <Card.Img variant="top" src={filename} />
            <Card.Body>
              <Card.Title>Card Title</Card.Title>
              <Card.Text>
                Some quick example text to build on the card title and make up the bulk of the card's content.
              </Card.Text>
              <Button variant="primary">Go somewhere</Button>
            </Card.Body>
          </Card>
        ))}
      </div>
    );
  };
  
export default About